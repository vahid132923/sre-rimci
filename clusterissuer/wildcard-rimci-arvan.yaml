apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-rimci-arvan
spec:
  secretName: wildcard-rimci-arvan
  issuerRef:
    name: arvancloud-issuer
    kind: ClusterIssuer
  commonName: '*.rimci.dev'
  dnsNames:
  - "*.rimci.dev"